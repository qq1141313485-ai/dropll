# 球镜项目主交接文档

更新时间：2026-08-23（Asia/Shanghai）

本文件是项目唯一的总览、决策和交接入口。它已合并当前线程、同一项目的历史
Codex 任务摘要、`竞球镜_项目合并上下文_20260816.md` 和仓库专项文档中仍有效的
内容。不要再以历史交接文档、旧 Mac 工程或发布产物作为当前状态的依据。专项规则
只在其对应文档维护，本文件只保留链接和结论，避免重复。

## 1. 当前基线

| 项目 | 当前结论 |
| --- | --- |
| 正式代码基线 | `C:\Users\JT\Documents\APP\cai_tool_app_build` |
| Git 仓库 | `main`，远程 `origin`：`https://github.com/qq1141313485-ai/dropll.git` |
| 当前版本 | `1.0.0+4`，显示名为“球镜”，Bundle ID / Android 包名为 `com.caimaster.jingqiujing` |
| 产品定位 | 足球赛事数据与资料整理工具；不售票、不代购、不提供投注、支付、充值、提现或资金服务 |
| API | `https://api.cclloo.com` |
| Web | `https://cclloo.com/app/` |
| Android APK | `https://cclloo.com/downloads/jingqiujing-1.0.0.apk` |
| 当前工作区 | 原文合集服务端仅向前同步已上线；客户端收藏与设置页备份/恢复优化均已提交但尚未发布，App 新入口仍关闭 |

Mac 历史工程、旧 APK、旧构建目录仅可用于排查，禁止反向覆盖正式基线。Mac 上供 iOS 构建的最新同步目录为：

```text
/Users/drop/Documents/Codex/2026-08-22/jingqiujing-c31b384
```

## 2. 已确认的产品规则

- 首版不做登录、支付、评论或用户上传。
- 比赛资料、赔率浏览、选号和奖金计算均为信息计算功能；不产生真实出票或代购。
- “10000 倍”是计算倍数，必须与真实票面区分；首版不提供拆票/拆单入口。
- 多场选择单场仅在全部已选玩法支持单场时可用；复合过关、胆码、保存方案保留。
- 保存方案只存本机：最近 7 天、最多 200 条、无云同步。
- 计划内容只展示已获授权的来源图片，保留来源水印；无真实数据时显示空/失败状态，禁止演示数据。
- 未识别计划名称进入 `pending_name`，不得自动创建大量错误计划。
- 名称识别使用低成本规则和管理员确认；当前决定不使用 OCR，生产环境保持关闭。
- App 不暴露同步、抓取、缓存等内部术语，异常提示面向普通用户。

## 3. 对话决策总表

### 3.1 选号与奖金

- 选号页不增加搜索、日期/联赛筛选、仅看已选、仅看支持单关等筛选工具；“更多玩法”和自动刷新不改。
- 顶部或底部持续显示已选场数、选项数、胆码数、最低过关要求和当前注数。
- 过关方式可选择复合过关；生成方案页的过关方式只展示，不能在该页二次修改。
- 比赛停售、接口暂时少返回或 SP 选项消失时，已选项不能静默移除，必须给出明确提示。
- 同场多玩法在组合计算中按市场软件的分支规则拆分；每个过关方式使用用户输入的同一倍数，不应臆测为平均分配倍数。
- “拆票”曾讨论过，但当前产品决定不做；不把没有实际出票意义的功能包装成出票方案。

### 3.2 比赛详情 UI

- 详情页的固定结构已定：`MatchHeader` 固定、`DetailTabs` 固定在头部下方、仅标签栏下方内容区独立纵向滚动；禁止改回整页滚动或 Sliver 吸顶。
- 固定头部的信息层级：联赛与场次、开赛时间、两队队徽与比分/VS、队名与比赛分钟；返回与分享按钮同导航层并避开灵动岛安全区。
- 详情页以 iPhone 15 Pro 的 Flutter 逻辑尺寸 `393 × 852` 作为主要像素验收基准；其他设备做响应式适配，不能把“所有设备逐像素相同”当作验收条件。
- 视觉基线为白底、深绿强调、紧凑数据表和细分割线；不使用大圆角卡片、重阴影或广告式模块。头部、标签高度、内容边距、表格列宽、字号和行高应以最终确认的真机效果逐屏验收。
- Android/Web 截图可用于核对信息结构和表格层级，但最终字体、安全区、间距和状态栏效果以 iPhone 15 Pro 真机截图为准；历史生图和第三方产品截图只作布局参考，不复制品牌、Logo、界面资产或未经授权内容。
- 未赛默认不展示“赛果”页；完场进入详情时默认展示赛果。详情页不提供“去选号”入口。
- “基本面”固定包含战绩、排名、状态、阵容、赛程五个二级栏目；缺少真实字段时隐藏对应内容或显示明确空态，不用占位数据补齐版面。
- 概览的正式模块顺序为：竞彩赔率、状态对比、历史交锋、重要缺阵。已存在 `_OddsSection`、`_FormComparisonSection`、`_HeadToHeadSection`、`_AbsenceSection`，后续应重构或修正它们，不再新增功能重复的第二套组件。
- 伤停只显示接口真实字段；没有具体原因时只能显示“伤病”“停赛”或“原因暂无数据”，禁止编造球员或伤停事实。
- 欧赔、亚赔、大小球是产品需要，但当前 API 契约明确尚未接入。没有独立、已授权数据源之前，不得用竞彩 SP 模拟这三类盘口。

### 3.3 数据与计划

- 计划抓取优先保证“错分少于漏分”：陌生或多作者标题保持 `pending_name`，经规则或人工确认后再重跑。
- 多作者文章由管理员对一篇真实文章逐图确认后保存顺序规则；以后仅对完整来源名称和
  图片数量都一致的文章复用，数量变化时退回待治理。来源顺序不稳定或证据不足时不保存规则、不猜测拆分。
- 当前明确不使用 OCR；计划名称和多计划拆分仅依赖规则及管理员确认。
- 队徽抓取/覆盖率是独立任务，先统计缺失、错配、下载失败和回退来源，再决定补齐策略。
- 比分“卡住”不能只看前端显示；必须区分上游未更新、服务端状态映射、实时链路断连和客户端缓存，并用接口与巡检记录验证。

### 3.4 备案、软著与合规

- 当前构建显示名为“球镜”，但备案最终是否受理、软著实名审核进度、最终上架名称均未在仓库中得到可验证的完成凭据，必须以各平台后台状态为准。
- 备案、安装包显示名、截图、隐私政策、支持页、软著和上架资料提交前必须一致；名称更改不等于备案或审核必然通过。
- 数据来源说明只能如实写公开只读 HTTPS 赛事/赛果/赔率资料及上游更新时间影响；不能把未授权数据写成自有数据，也不能承诺官方数据源。
- App Store 与各地区的赛事数据、彩票相关计算、营销文案与内容授权存在合规风险；发布前需要独立核对，不以技术实现替代合规结论。

### 3.5 官网、Web 与服务器边界

- 官网根目录与 Flutter Web 必须分开发布：官网为 `/www/wwwroot/cclloo.com`，Flutter Web 为 `/www/wwwroot/cclloo.com/app/`。不得将 Web 构建解压到官网根目录。
- API 是独立反向代理服务。部署官网或 `/app/` 时，禁止覆盖 API 配置、API 服务目录或下载目录。
- Flutter Web 必须保持国内网络可用：CanvasKit 和 Noto Sans SC 使用站内静态资源，不能在运行时依赖 Google、gstatic 或 Google Fonts。每次 Web 发布后都要在真机网络环境检查首屏、中文字体和接口加载。
- 官网文案应保持“赛事观察、资料整理、个人复盘”定位，不使用保证中奖、稳赚、必胜、诱导投注或夸大预测能力的表达。
- 官网预约、账号或后台等能力若只存在浏览器 `localStorage`，必须明确视为前端临时能力，不得描述为服务器已落库或有运营后台。

### 3.6 历史结论的取舍

- 2026-08-16 合并稿中“后端、真实接口和管理后台尚未完善”是当时快照，已被后续 API、计划内容服务和管理能力开发覆盖，不能作为当前结论。
- 历史“46 项 Flutter 测试通过”“22 项奖金测试通过”等数字不代表今天的完整测试状态；当前只有本次实际执行并记录的测试结果才可用于发布判断。
- 历史工作名称“竞球镜”与当前构建显示名“球镜”并存。代码显示名是事实；备案、软著、上架最终名称是否统一仍待申请后台和最终提交资料核对。
- “官网已部署”“Web 已恢复”“APK 已上传”等部署结论均带时间性。后续发布前重新请求线上地址验证，不用历史聊天截图替代验收。

## 4. 已完成且已验证

### 跨端与发布

- Flutter 共用代码维护 iOS、Android、Web；生产 API 已使用 HTTPS。
- 2026-08-21 已构建 Android Release APK 和 Web Release，并部署 Web 到 `/app/`；官网根目录已恢复原官网。
- iOS 最新代码已同步到 Mac，`flutter pub get` 成功，`flutter build ipa --release --no-codesign` 归档成功。
- Mac 已配置 Apple Development 证书；iPhone 15 Pro 已被 Xcode/Flutter 识别。
- 2026-08-21 已通过 `flutter run --release` 成功签名、安装并启动 iPhone 15 Pro。该结果只代表开发签名真机安装成功，不等于 TestFlight 或 App Store 发布验收完成。
- 2026-08-22 已将来源 commit `c31b384` 以全新目录同步到 Mac；源码包 SHA-256 为 `afa677ba480fcb27f59003b68dc0952f85cfafffba1fcedecf5c7b80104f18da`。Mac 定向静态检查无问题、完整 Flutter 测试 54 项通过。SSH 签名最初受登录钥匙串 `errSecInternalComponent` 阻塞；用户在 Mac 前台授权后，开发签名 Release 的签名验证、安装和启动均成功。用户已在 iPhone 15 验证真实名称命中、不存在名称空结果及清空后恢复近期/历史浏览，计划名称搜索专项通过；该结果仍不等于整份真机回归、TestFlight 或 App Store 验收通过。

### 奖金计算

- 奖金算法改动已提交为 `94fe7e7`（`fix: expand mixed-play betting branches`）。
- 已实现复合过关按玩法分支展开、同场多玩法在复合过关中的组合拆分，以及同一场其他玩法不重复放大单场。
- 2026-08-22 奖金引擎测试 25 项通过，Flutter 完整测试 49 项通过；已用外部平台截图回归：`5串26` 为 52 注、104 元、最高奖金 `7551.49`；同场增加一个选项后为 67 注、134 元、最高奖金 `8539.85`。
- 奖金引擎与对应测试的定向静态检查为 0 问题；全项目 `flutter analyze` 仍有 9 项详情页既有 lint 警告，因此不能写成全项目静态检查通过。
- 该算法已做针对性验证，但尚未完成覆盖真实全玩法、异常赔率和全部过关方式的长期回归；不要在未新增用例前继续改核心公式。

### 数据与内容能力

- 比分链路具备实时监听、数据库同步、官方回退、动态分钟及完场纠正能力。
- 比赛详情、历史赔率、分析资料、队徽、选号、保存方案与完场结算均已有实现。
- 计划服务具备公开接口、管理后台、定时同步、规则别名、`pending_name`、重试队列、旧 ID 重定向及收藏迁移能力。
- 计划名称、近期/历史和指定 ID 筛选的 SQL 条件位置错误已在 commit `cc653cd` 修复；App 有关键词时改为跨近期与历史全库搜索，发布冒烟检查会同时验证真实名称命中和不存在名称返回空结果。
- 详情页已完成固定比赛头部、固定标签栏和独立可滚动内容区的架构；概览内容已开始按真实字段绑定。后续继续 UI 时不得破坏固定头部与固定标签栏的滚动边界。

### 已处理的运维故障

- 曾发生 Flutter Web 被错误覆盖到官网根目录、`/app/` 返回 404 的事故。当前部署规则已在“官网、Web 与服务器边界”固定，发布前必须备份并按目标目录部署。
- 曾发生 Flutter Web 在国内网络因 Google CanvasKit / 字体加载失败而空白或中文方框。代码已内置 Noto Sans SC，并要求 CanvasKit 走站内资源；是否持续在线有效需按每次部署复验。
- Mac 远程构建曾受局域网/VPN 路由和钥匙串签名权限影响。网络恢复后已完成真机安装；后续 SSH 构建仍需核对设备连接、Apple 账号、provisioning profile 与钥匙串权限。

## 5. 当前未完成与风险

按优先级执行，不并行混改：

1. **真机稳定性验收**：连续验证比分状态、完场纠正、计划加载、弱网/断网、保存方案与结算；完成 7 天记录后再评估公开推广。
2. **奖金算法扩展回归**：补齐真实样本和边界用例，尤其是多场、多玩法、复合过关、赔率缺失/停售及最低过关限制。任何数值改动必须提供可复现对照。
3. **方案抓取治理**：优化规则识别、异常图片前置过滤和 `pending_name` 长期闭环；不启用 OCR。
4. **队徽覆盖率**：独立检查缺失率、错误映射与图片下载失败，不与计划识别改动混合发布。
5. **详情页 UI**：继续“基本面”和“赔率”标签页前，先保持现有固定头部/标签栏不变；所有新增模块必须真实数据优先，缺失时隐藏或显示明确空态。
6. **合规与发布资料**：App 备案名称尚需最终确认；软著实名审核、内容授权、数据来源合规、App Store 隐私问卷和素材均未形成最终闭环。

以下结论尚未充分验证，不能写成“已稳定”：长期比分准确性、方案抓取完整率、队徽覆盖率、OCR 分类准确率、各平台 UI 一致性、App Store 审核可通过性。

2026-08-22 已完成稳定性验收启动前检查：生产公开比分与计划接口单次巡检通过，客户端 54 项测试通过，其中 4 项覆盖保存方案的 7 天清理、200 条上限、旧格式兼容和删除持久化；隐私、支持和 Web 页面可访问。服务器比分巡检和计划同步定时器已确认活跃，但历史 2300 条记录中有 795 条带警告，尚无 TestFlight 发布候选构建，因此连续 7 天仍未起算，真机弱网/断网、强制退出后保存恢复和结算仍未验收。详细证据见 `SEVEN_DAY_STABILITY_ACCEPTANCE.md`。

2026-08-22 已将计划名称、活动范围和指定 ID 筛选修复部署到生产 API。服务器端 `verify_plan_release.py --require-data` 总验收退出码为 0，Windows 外部复验也通过：不存在名称返回 0 条，近期与历史结果分离，指定 ID 只返回对应计划。部署检查仍有 `pending_name=1240`、失败文章 2 条，来源同步 dry-run 另识别 69 个待映射名称；这些治理警告不影响本次搜索修复，但必须在方案抓取治理中继续闭环。

2026-08-22 17:07 已将同步治理指标部署到生产：管理后台会分开展示待治理文章、去重名称、未提取名称、近 24 小时新增、7 天以上积压、失败文章及“启用 / App 可见”计划数。部署后动态快照为待治理 1247 篇、可提取名称 507 个、未提取名称 348 篇、近 24 小时新增 65 篇、7 天以上积压 827 篇、失败 2 篇、重试队列 0；启用计划 361 个且 361 个均有有效图片可见。该快照会随五分钟定时同步变化，不能把文章数直接当作缺失计划数。相关提交为 `8ff5094`、`8eb4e97`；服务器端 9 项相关测试、部署检查及公开接口 required-data smoke 均通过。

2026-08-22 17:36 已将非技术版计划内容后台部署到生产，提交 `2d15daf`。页面以“今天是否需要处理”为入口，失败文章优先展示，待确认文章通过新增的 `/v1/admin/plan-sync/pending-names` 按名称聚合并显示一次操作影响篇数；计划列表默认仅显示正常计划并支持名称搜索，合并操作使用三步说明和独立确认弹窗，手动上传与系统检查默认折叠。桌面、390px 窄屏和 `cc -> CC` 合并预览均已完成模拟视觉验证，没有执行真实合并。服务器端 10 项相关测试、聚合接口生产检查和公开接口 required-data smoke 通过；部署时动态数据为待确认名称 508 个、未提取名称文章 348 篇、失败文章 1 篇。

2026-08-22 18:07 已将管理员逐图拆分入口部署到生产，提交 `4f209c5`、`691ee30`。待确认名称卡片新增“一条有多个计划”：管理员对一篇真实文章逐张选择计划或跳过无关图片，保存后按完整来源名称和图片数量复用规则，并把当前同名文章加入重跑队列；图片数量变化会退回 `pending_name`，不会强行归类。该流程不读取图片文字，生产 `CAIMASTER_PLAN_OCR_ENABLED=0` 保持不变。桌面和 390px 窄屏完成模拟视觉验收；服务器端 16 项相关测试、真实待处理文章图片读取、管理页按钮、公开接口 required-data smoke、API 服务及五分钟 timer 均验证通过，重启后无告警。生产验收只调用读取接口，没有保存真实拆分规则。

2026-08-22 18:16 已将待确认名称和逐图拆分中的完整计划下拉框替换为“搜索或新建计划”，提交 `bfa45a9`。输入关键词最多展示 8 个已有计划，精确匹配显示“将归入已有计划”，不存在的名称显示“将创建新计划”；不需要预先到手动新增区域操作。桌面及 390px 窄屏完成模拟视觉验收，服务器端 16 项相关测试通过。部署后第一次公开接口 smoke 恰逢服务重启返回一次 502，立即重试全部通过，服务日志无告警，API 与五分钟 timer 均为 active。

2026-08-22 18:56 根据真实操作反馈，已将浏览器原生计划提示替换为页面内的可点击下拉面板，提交 `6555acb`。面板会列出最多 8 个已有计划并提供“选择”按钮；输入不存在的完整名称时显示黄色“＋新建”按钮。待确认卡片和逐图拆分弹窗共用此组件，桌面与 390px 窄屏均完成搜索、选择已有、新建名称和弹层位置验证。服务器端 16 项相关测试通过，生产页面已确认包含选择与新建控件；重启后的即时 smoke 出现一次短暂 502，随后 required-data smoke 全部通过且服务日志无告警，API 与五分钟 timer 均为 active。

2026-08-23 已完成“原文整篇同步、完整版本、稳定收藏”：客户端功能提交为 `c97841a`；服务端最终保护提交为 `0c42455` 并已部署。一篇来源文章始终作为一个合集，标题、图片内容或顺序变化时新增完整版本，旧版本不覆盖。产品决定明确为“不迁移之前的文章，只从启用时刻向前同步”；生产 `CAIMASTER_PLAN_SOURCE_MIRROR_SINCE=1787419914`（2026-08-23 01:31:54 +08:00），镜像已开启，未设置有效起点时同步和部署检查会拒绝运行，避免意外搬运全部历史。试运行期间产生的 20 篇合集、20 个版本及 69 张图片已全部清理，共删除 138 个原图/缩略图文件；20 条旧同步记录从部署前数据库备份恢复，旧计划、旧更新和旧图片数量未变化。清理后新原文三张表均为 0，首轮向前同步候选 0、失败 0。生产 `CAIMASTER_PLAN_ARTICLES_PUBLIC_ENABLED=0` 继续关闭，因此当前 Web/App 仍使用旧计划接口；客户端的原文标题搜索、完整版本、新版提醒、多图命名收藏等界面尚未发布。Flutter 完整测试 59 项通过，改动文件定向静态检查 0 问题；服务器最终包安装前 31 项门禁通过，发布后 `verify_plan_release.py --require-data`、旧计划真实数据冒烟、API 与同步 timer 均通过，API 最近日志无告警。

2026-08-23 设置页第一轮优化已完成并提交为 `c665907`，尚未发布：新增“我的数据”，分别显示内容收藏、关注更新和最近 7 天保存方案的真实数量；三类数据可独立清空并有明确二次确认，删除一类不会影响其他两类，也不会影响原网站内容。每次重新进入设置页会刷新数量；页面明确说明数据只保存在本机，并区分用户收藏与图片缓存。重复的“问题反馈”入口已合并到服务中心；版本号不再写死，移动端读取安装包信息，Web 读取构建生成的 `version.json`，393×852 实测显示 `1.0.0（构建 4）`。定向静态检查 0 问题，Flutter 完整测试增加至 61 项并全部通过，正式 `/app/` Web Release 构建成功；窄屏首屏、滚动、禁用/可用删除按钮和确认弹窗均完成浏览器验收，验收中未确认删除任何本机数据。

2026-08-23 设置页第二轮优化已完成并提交为 `a5f6ec2`，尚未发布：新增“备份本机数据”和“从备份恢复”。备份只导出内容收藏、关注更新、最近 7 天保存方案三类本机数据，不包含账号或服务端凭据；损坏、过期和重复条目会先过滤。恢复采用合并补充，恢复前展示三类有效数量并二次确认；本机现有数据优先，同一收藏、关注或保存方案不会重复导入，不会整批覆盖。备份格式带固定标识和版本号，错误文件及不支持的版本会给出可理解提示。新增 `file_selector 1.1.0` 负责跨平台选取备份文件，沿用 `share_plus` 导出。定向静态检查 0 问题，Flutter 完整测试增加至 64 项并全部通过，正式 `/app/` Web Release 构建成功；本地浏览器目视确认新增入口、说明文案和滚动布局正常，验收中未实际触发导出或导入。

2026-08-23 备份/恢复实际 Web 验收后补充修正提交 `2577af8`，仍未发布：Web 端由系统分享改为直接下载 JSON，iOS/Android 继续使用系统分享。首次本地测试暴露新文件组件未正确注册，执行干净重建后错误消失；因此增加文件组件后不能只依赖增量构建。隔离本地网址中真实选择临时备份，预览正确显示内容收藏 0、关注更新 1、保存方案 0；首次确认后新增 1 项并刷新数量，再次导入同一文件显示“没有需要补充的新内容”且数量保持 1。临时文件和隔离测试页均已清理，未改生产或用户正式数据。内置验收浏览器没有提供程序下载文件的可读取路径，因此导出文件内容尚未被人工重新打开；当前 Windows 未连接手机，iOS/Android 系统分享与真机恢复仍待 Mac 连接 iPhone 后确认。修正后设置页定向静态检查 0 问题、相关 8 项测试通过、正式 `/app/` Web Release 干净构建成功；此前完整 64 项测试仍全部通过。

当前计划内容服务生产包来源 commit 为 `0c42455`，本地文件为 `dist/caimaster-plan-content-20260823-013055.tar.gz`，大小 79,534 字节，SHA-256 为 `cacca464068a641ce1df7bb5418b6c6dafba8b5288fa352c3057530a4eaa6325`。部署前 SQLite 在线备份为 `/opt/caimaster-api/backups/plans-before-article-mirror-20260823-011657.db`，SHA-256 为 `d868e94def22882677d98e9432d57ec6774bd3584b17f37843c5d9a82b2e7d9d`。更早的 `010843`、`012528` 包仅为本轮候选，不得再次使用。

## 6. 版本控制与构建纪律

- 当前 `main` 的原文合集客户端、服务端、发布门禁、仅向前同步保护及设置页备份/恢复优化均已提交；服务端已部署，客户端尚未发布，公开入口保持关闭。奖金算法、Web 启动模板、保存方案回归和计划名称筛选修复也均已提交。`outputs/` 中原有的分类审阅材料继续跟踪，只有明确的发布、备案和 Web QA 生成目录被忽略。
- 2026-08-22 已用 Flutter 3.44.4 对来源 commit `e772d05` 执行 `flutter build web --release --base-href /app/`：构建成功，模板占位符已替换，启动脚本语法通过，`/app/` 基路径正确，CanvasKit 明确从站内 `canvaskit/` 加载。此次仅为本地编译验证，未部署、未推送；线上状态仍须发布时复验。
- 提交前应区分“源代码改动”和“构建产物”，不要把生成的发布/验收目录、`build/`、`.dart_tool/` 或密钥写入 Git；不要整体忽略 `outputs/`，以免遗漏应跟踪的审阅证据。
- 奖金算法提交前应执行定向静态检查、完整测试和 `git diff --check`，并保留外部对照样本与结果；发布前仍须清理或明确豁免全项目静态检查中的既有警告。
- 每次发布前递增构建号、记录构建时间、来源 commit 和目标平台；没有 commit 的构建只能称为测试构建。
- 任何服务器修改先做时间戳备份；不得把服务器密码、Apple ID 密码、证书私钥、API Token 写入代码、文档或提交记录。

## 7. 环境与部署

### Windows 开发机

```text
代码：C:\Users\JT\Documents\APP\cai_tool_app_build
Flutter：C:\Users\JT\Documents\Codex\2026-07-01\python-sqlite3-caimaster-core-db-matches-2\work\toolchain\flutter
```

### Mac iOS 构建机

```text
macOS 26.5.2
Xcode 26.6
Flutter 3.44.8 / Dart 3.12.2
Flutter：/Users/drop/development/flutter/bin/flutter
项目：/Users/drop/Documents/Codex/2026-08-21/jingqiujing-latest
```

通过 SSH 构建时，macOS 登录钥匙串可能导致 `errSecInternalComponent`。先在 Mac 前台登录 Xcode，并确保 Development 证书私钥可访问；只在用户本地已授权的会话中解锁钥匙串，不要保存明文密码。

### 线上服务器

```text
Web 根目录：/www/wwwroot/cclloo.com
API：https://api.cclloo.com（独立反向代理，禁止被 Web 部署覆盖）
计划内容服务：/opt/caimaster-api
```

已知备份：`/www/wwwroot/cclloo.com.backup-20260821-192337.tar.gz`。该备份仅用于 2026-08-21 Web 部署回退，不替代常规发布前备份。

## 8. 专项文档索引

| 主题 | 唯一专项文档 |
| --- | --- |
| API 字段与接口约定 | `API_CONTRACT.md` |
| 数据库字段 | `docs/db.md` |
| 数据源登记与合规状态 | `docs/DATA_SOURCE_REGISTER.md` |
| 方案内容同步与后台 | `docs/PLAN_SOURCE_SYNC.md`、`server/plan_content/README.md` |
| 计划内容管理行为 | `docs/PLAN_CONTENT_ADMIN.md` |
| 计划名称治理待确认清单 | `docs/PLAN_NAME_CLASSIFICATION_2026-07-27.md` |
| 上架阻塞项与素材 | `docs/RELEASE_CHECKLIST.md` |
| iOS 真机验收项 | `docs/IOS_DEVICE_REGRESSION.md` |
| 7 天稳定性验收 | `docs/SEVEN_DAY_STABILITY_ACCEPTANCE.md` |
| 构建与发布产物台账 | `docs/RELEASE_ARTIFACTS.md` |
| 备案填写事实清单 | `docs/APP_FILING_MATERIALS.md` |
| 隐私与内容权利 | `docs/PRIVACY_POLICY.md`、`docs/CONTENT_RIGHTS_EVIDENCE.md`、`docs/CONTENT_AUTHORIZATION_TEMPLATE.md` |
| 历史跨窗口汇总来源 | `C:\Users\JT\Documents\Codex\2026-07-16\app\outputs\竞球镜_项目合并上下文_20260816.md`（只读历史快照） |

`HANDOFF_2026-07-22.md`、`docs/MAC_CODEX_CONTEXT_MERGE_2026-08-03.md`、
`docs/changes.md`、`docs/spec.md`、`docs/field_map.md` 和
`docs/PLAN_APP_CONNECT.md` 为历史索引，不得作为当前实施依据。

## 9. 文档治理与下一位执行者的开始顺序

### 文档治理

- `PROJECT_HANDOFF.md` 是唯一总交接文档。产品决策、环境变化、发布事实和优先级变更先更新这里，再更新对应专项文档。
- `HANDOFF_2026-07-22.md`、`docs/MAC_CODEX_CONTEXT_MERGE_2026-08-03.md`、
  `docs/changes.md`、`docs/spec.md`、`docs/field_map.md`、
  `docs/PLAN_APP_CONNECT.md` 只保留历史索引；不得恢复为第二份当前状态文档。
- 临时排查结论只进入 issue、日志或对应专项文档；未验证的信息必须标记“待核实”，不写进“已完成”。
- 同一个事项只保留一个负责人状态和一个下一步，不创建“Windows 版待办”“Mac 版待办”等平行分支。

### 开始顺序

1. 阅读本文件、`git status`、`git log -8`，确认本地工作区没有被旧工程覆盖。
2. 复核提交 `94fe7e7` 的奖金算法对照样本与测试结果，并检查全项目静态分析的既有警告。
3. 检查线上 API、Web 和计划同步的健康状态；仅收集证据，不先改数据或公式。
4. 在一场真实未赛、进行中和完场比赛上完成一次端到端验收，记录截图和接口结果。
5. 只选择一个优先级任务实施，并同步更新对应专项文档与本文件的状态。
