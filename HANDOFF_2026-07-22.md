# 竞球镜 iOS/App 与服务器交接文档

更新时间：2026-07-24 18:00 CST

## 一句话现状

当前 App 已能连接线上 API，首页比赛列表、赛果、可投注接口可正常返回；球探实时比分链路已接入并修过一次“比分/分钟卡住”的问题。当前仍建议继续重点观察球探同步去重、绑定覆盖率、分享图是否符合产品预期。

## 本地项目

- Flutter 项目路径：`/Users/drop/Documents/Codex/2026-07-21/q/work2/cai_tool_app`
- Xcode 项目：`/Users/drop/Documents/Codex/2026-07-21/q/work2/cai_tool_app/ios/Runner.xcodeproj`
- Flutter 工具链：`/Users/drop/Documents/Codex/2026-07-21/q/toolchains/flutter/bin/flutter`
- App 不读取 `.dev-api-token`；赛事接口为公开只读，安装后无需注册或激活
- 该目录现已初始化 Git；提交前仍应先检查 `git status`，不要覆盖用户未提交的改动。

常用检查命令：

```bash
cd /Users/drop/Documents/Codex/2026-07-21/q/work2/cai_tool_app
/Users/drop/Documents/Codex/2026-07-21/q/toolchains/flutter/bin/flutter analyze
/Users/drop/Documents/Codex/2026-07-21/q/toolchains/flutter/bin/flutter test
```

最近一次检查结果：

- `flutter analyze`：通过，0 问题
- `flutter test`：18 个测试全部通过

## 线上服务器

- 服务器：`8.137.124.99`
- SSH 用户：`root`
- 密码：不要写入文档；从用户或安全渠道获取
- API 地址：`https://api.cclloo.com`
- API 服务目录：`/opt/caimaster-api`
- API 主文件：`/opt/caimaster-api/app.py`
- 数据库：`/opt/caimaster/caimaster_core.db`
- 球探同步目录：`/opt/caimaster`
- 球探监听日志：`/opt/caimaster/work/titan_shadow.jsonl`

核心服务：

```bash
systemctl status caimaster-api.service --no-pager
systemctl status caimaster-titan-shadow.service --no-pager
systemctl status caimaster-titan-db-sync.service --no-pager
```

最近确认状态：

- `caimaster-api.service`：active
- `caimaster-titan-shadow.service`：active
- `caimaster-titan-db-sync.service`：active

公开数据机制：

- App 打开即可请求公开只读赛事、赔率、赛果和模型展示接口。
- API 使用 HTTPS，Uvicorn 仅绑定 `127.0.0.1`，Nginx 对每个来源限流为每秒 2 次并允许短暂突发。
- App 不保存账户信息，方案与结算结果仅保存在用户本机。

## 已做的重要修复

### App 侧

主要文件：

- `lib/widgets/home/home_page_container.dart`
- `lib/widgets/home/home_match_list.dart`
- `lib/widgets/home/home_match_row.dart`
- `lib/models.dart`
- `lib/selection_page.dart`

已做内容：

- 首页按比赛状态自适应刷新：有进行中/中场比赛时每 5 秒刷新，否则每 30 秒刷新。
- 首页排序调整为：比赛中/中场在未赛前面；比赛中和中场内部严格按比赛编号升序，不再因为中场置顶。
- 已修正“分析”按钮为“分享”，生成方案页标题也已改为“竞球镜·方案分享”。
- 奖金优化页支持倍数输入，优化预算会跟随倍数变化。
- 键盘支持点击空白和拖动列表收起。
- 选号页 SPF/让球 SPF 小格子边框样式已调整；单关红色边框按整组包裹。
- 更多玩法页面做过紧凑化；比分排序按下拉列表顺序。

### 服务器/API 侧

主要文件：

- `/opt/caimaster-api/app.py`
- `/opt/caimaster/titan_live_sync.py`
- `/opt/caimaster/titan_db_sync.py`

已做内容：

- 修复完场比赛仍显示比赛中的问题：官方赛果 JSON 有 `sectionsNo999` 或 `matchResultList` 时，API 强制视为 `finished`，返回 `liveStatusText = 完场`。
- 修复周二206一直卡在进行中：已确认 `/v1/matches/live` 不再返回周二206，`/v1/matches/results` 返回周二206 `FINISHED/完场/1:4`。
- 球探 WebSocket 监听关闭主动 ping，减少 `keepalive ping timeout`。
- 球探断线后最大重连等待从 60 秒降到 8 秒。
- DB 同步从只处理 `zqlivechange` 改为同时处理 `change_ut_xml` 和 `zqlivechange`。
- 修正球探 `change_ut_xml` 时间格式：月份是 JS 风格，从 0 开始，例如 `2026,6,22` 表示 2026-07-22。
- 新增 `matches.titan_state_time` 字段，API 返回实时分钟时按当前服务器时间动态计算，不再固定读旧 `live_minute`，解决分钟长期卡住。
- 球探赛程目录改为合并 `Schedule_0_2.txt`、`Schedule_0_3.txt`、`Schedule_0_4.txt`；目录由 10 场扩展为 35 场。
- 补充已人工验证的一对一队名别名（例如“迈国际/迈阿密国际”“巴竞技/巴拉纳竞技”），严格绑定模拟从 2/20 提升为 10/20；线上带球探编号的竞彩记录目前为 17 条。
- 服务重启回放多个影子日志文件时共用去重缓存，避免日志轮转边界重复写入镜像事件。

服务器备份文件：

- `/opt/caimaster-api/app.py.bak-official-results-20260722-001`
- `/opt/caimaster-api/app.py.bak-official-results-20260722-002`
- `/opt/caimaster-api/app.py.bak-20260722-dynamic-minute`
- `/opt/caimaster/titan_db_sync.py.bak-20260722-rt-latency`
- `/opt/caimaster/titan_db_sync.py.bak-20260722-state-time`
- `/opt/caimaster/titan_db_sync.py.bak-20260722-js-month`
- `/opt/caimaster/titan_db_sync.py.bak-20260722-dynamic-minute`
- `/opt/caimaster/titan_live_sync.py.bak-20260722-rt-latency`

## 线上验证命令

可在服务器上运行已有临时检查脚本：

```bash
python3 /tmp/check_caimaster_counts.py
python3 /tmp/check_caimaster_api.py
```

如果临时脚本不存在，可直接通过本机 HTTPS 反代检查公开接口，例如 `curl -sS https://api.cclloo.com/v1/matches/live?limit=1`。

常用日志：

```bash
journalctl -u caimaster-api.service -n 80 --no-pager
journalctl -u caimaster-titan-shadow.service -n 80 --no-pager
journalctl -u caimaster-titan-db-sync.service -n 120 --no-pager
tail -n 80 /opt/caimaster/work/titan_shadow.jsonl
```

常用 DB 检查：

```bash
sqlite3 /opt/caimaster/caimaster_core.db "
select match_no, official_match_id, titan_match_id, live_status, live_minute,
       titan_state_time, live_home_score || char(58) || live_away_score as score,
       titan_last_updated
from matches
where titan_match_id is not null
order by titan_last_updated desc
limit 20;
"
```

## 当前已知问题/后续优先级

1. 球探事件重复写入  
   `change_ut_xml` 和 `zqlivechange` 常常是同一事件的两个通道。同步器已按比分、状态、半场比分和标准化状态时间去重，并在日志回放间共享缓存；仍应观察真实比分纠错（例如 VAR 取消进球）不会被误杀。

2. 球探绑定覆盖率仍不完整
   完整目录与别名已将严格匹配提升到 10/20；其余比赛在球探目录中尚无同时间候选，不能仅按开赛时间绑定。未绑定比赛仍只能走官方/竞彩数据。

3. 分享图与产品期望仍有偏差  
   当前生成方案页的“分享”是用方案文字重新绘制 PNG，不是完整截取当前选号页 UI。如果要求“把当前选号页生成分享图片”，需要用 `RepaintBoundary` 包住选号页核心区域，或者设计专门的分享卡片组件复用当前选项数据。

4. App 首页刷新已自适应
   有 live/中场比赛时每 5 秒刷新；无 live 比赛时每 30 秒刷新。继续观察高峰期的接口负载。

5. 线上 API 仅通过 HTTPS 反代对外提供
   Uvicorn 已绑定 `127.0.0.1:8787`；Nginx 提供 `https://api.cclloo.com`。认证接口有来源限流，仍建议后续增加 Nginx 层限流与告警。

## 换账号接手建议

1. 先确认 Xcode 登录新 Apple ID 后能打开：
   `/Users/drop/Documents/Codex/2026-07-21/q/work2/cai_tool_app/ios/Runner.xcodeproj`
2. 用项目自带 Flutter 跑：
   `/Users/drop/Documents/Codex/2026-07-21/q/toolchains/flutter/bin/flutter analyze`
3. 跑测试：
   `/Users/drop/Documents/Codex/2026-07-21/q/toolchains/flutter/bin/flutter test`
4. 真机运行前确认 API 服务 active；不要创建、读取或传入 `.dev-api-token`。
5. 安装到手机后，首页看：
   - 比赛中/中场是否排在未赛前
   - 完场比赛是否进入赛果页
   - 比分分钟是否不再长期卡死
   - 选号页单关红框、SPF/让球 SPF 边框、更多玩法紧凑度
   - 奖金优化倍数和预算联动
   - 分享按钮能否弹出系统分享面板

## 关键结论

“比分卡住”之前主要不是 App 卡，而是服务器把实时分钟存成固定文本，以及球探 WebSocket/DB 同步链路有断线和通道处理问题。现在已改成 API 动态计算实时分钟，比分本身仍取决于球探是否推送新比分事件。

## 2026-07-25 计划内容后台

- 新增独立计划数据库 `/opt/caimaster-api/plans.db`，不影响只读赛事库。
- 新增图片目录 `/opt/caimaster-api/plan-media`，上传时生成原图和缩略图。
- 新增服务器模块 `/opt/caimaster-api/plans.py` 和管理页 `/opt/caimaster-api/plan_admin.html`。
- 管理入口为 `https://api.cclloo.com/admin/plans`，管理员密钥仅存服务器 `api.env`，不要写入项目或交接文档。
- 公开接口包括 `/v1/plans`、`/v1/plans/recent`、`/v1/plans/{id}/updates` 和 `/media/plans/{filename}`。
- App 计划页已支持线上分页、搜索、今日更新状态、详情批次和原图下载；没有正式上传内容时回退到内置演示数据。
- 详细说明见 `docs/PLAN_CONTENT_ADMIN.md`。

## 2026-07-23 历史赔率

- 服务器采集器 `/opt/caimaster/caimaster_core.py` 已增加 `odds_snapshots` 表：每 30 秒采集周期只在五类官方 SP 实际变化时写入快照，使用赔率内容哈希去重。
- API `/opt/caimaster-api/app.py` 已增加公开只读的 `GET /v1/matches/{match_id}/odds-history`，按时间正序返回快照，并由 Nginx 限流保护。
- App `api_client.dart` 与 `match_detail_page.dart` 已接入：赔率页展示真实初始 SP、即时 SP 和累计变化次数。首批基线于 2026-07-23 15:51 写入；更早历史不存在，后续随官方 SP 变化自动积累。
- 上线前已分别备份两个服务器文件，服务 `caimaster.service` 和 `caimaster-api.service` 均已重启并处于 active。

## 2026-07-26 全链路巡检

- 发现 `caimaster-titan-shadow.service` 虽为 active，但
  `titan_shadow.jsonl` 从 7 月 23 日起没有新事件。原因是 WebSocket
  连接静默失活后，监听器每 30 秒继续等待，不会重新获取 token 和订阅。
- 线上 `/opt/caimaster/titan_live_sync.py` 已增加 300 秒无任何数据帧时的
  `idle_reconnect` 自愈。Titan 不稳定响应 WebSocket ping，因此保持协议 ping
  关闭，以实际数据帧作为健康判断。备份为
  `/opt/caimaster/titan_live_sync.py.bak-20260726-0139`。
- 修复后事件文件持续写入，三个服务
  `caimaster-api`、`caimaster-titan-shadow`、`caimaster-titan-db-sync`
  均为 active。以后必须同时检查文件 mtime，不能只看 systemd 状态。
- 确认只有 `titan_db_sync.py` 写比赛库；`titan_live_sync.py` 只写 JSONL，
  两通道事件由 DB 同步器去重，不是两个进程重复写 DB。
- App 奖金算法已修正同场混合玩法可同时命中时的最高奖金累计，并增加测试。
- 保存方案的比分玩法结算已改用竞彩固定比分表，不再依赖完场接口是否仍返回
  CRS 赔率，避免把标准比分误判成“胜其他/平其他/负其他”。
- 计划同步仍有高优先级数据治理问题：线上 `plan_source_map.json` 还是旧版
  三条别名格式，等价于允许自动建计划。已出现“7.25鸿哥晚场”“毒液晚场更新了”
  等不稳定计划 ID，会导致收藏无法跟随次日更新。处理前先备份 `plans.db`，再使用
  结构化规则将 `allowAutoCreate` 设为 `false`，人工确认别名并合并重复计划；不要
  根据模糊标题自动猜作者。

## 2026-07-26 计划名称治理第一阶段

- 线上两天数据共 361 篇已导入文章、318 个计划；276 个计划只有一次更新，
  已确认旧同步器基本把每日文章标题直接当成计划名称。
- 已暂停同步并备份：
  `/opt/caimaster-api/plans.db.bak-20260726-0907-name-governance` 和
  `/opt/caimaster-api/plan_source_map.json.bak-20260726-0907-name-governance`。
- 已部署最新版计划后台和同步器，数据库增加原始名称、解析名称、待审核标记、
  同步运行记录及重试队列；管理后台和公开 App 接口 smoke 均通过。
- 线上名称规则已切换为结构化格式，`allowAutoCreate=false`。现有 318 个名称
  仍允许继续更新，未知标题只进入 `pending_name`，不会自动创建新计划。
- 修复 `install_plan_content.sh`：初始化 `api.env` 后必须加载环境变量再运行
  部署检查，避免把已配置的管理员密钥和同步开关误报为缺失。
- 暂未合并现有重复计划。App 收藏保存的是计划 ID，直接停用或删除旧 ID 会让
  用户收藏失效。第二阶段应先增加旧 ID 到标准计划 ID 的别名/重定向机制，再做
  可回滚的更新和图片迁移。

## 2026-07-26 计划 ID 兼容基础设施

- 线上计划数据库已增加 `plan_aliases`，旧计划 ID 可重定向到标准计划 ID；
  `/v1/plans?ids=旧ID` 和 `/v1/plans/旧ID/updates` 均返回标准计划。
- App 计划模型已支持 `aliasIds`。收藏刷新成功后会把本地旧 ID 自动迁移成标准
  ID，避免计划合并后收藏消失。
- 管理后台新增重复计划合并功能，必须先预览更新数和图片数，再明确确认执行；
  合并事务会迁移更新及同步文章引用、停用来源计划并保留旧 ID 重定向。
- 同步器按旧名称找到已合并计划时会写入标准计划，不会重新激活来源计划。
- 部署流程现在会在安装前使用服务器虚拟环境运行 `test_plan_aliases.py`；2 项
  临时数据库合并测试、公开 API smoke 和 required-data smoke 均通过。
- 本轮上线前备份：
  `/opt/caimaster-api/plans.db.bak-20260726-121026-alias-infra`，并同时备份了
  当时的 `plans.py` 和 `plan_source_sync.py`（相同时间戳后缀）。
- 当前生产 `plan_aliases` 行数为 0，尚未合并任何真实计划。下一步只能从明确的
  重复候选开始人工预览和确认，禁止按模糊名称批量自动合并。

## 2026-07-26 比赛详情分析一期（已部署）

- 实际核验竞彩网当前详情页及页面请求，确认可获取：对阵资料、历史交锋、
  主客队积分、双方近期战绩、未来赛程、射手、伤停、竞彩固定奖金历史，以及
  完场事件和技术统计。竞彩网不提供真正的欧赔、亚赔和大小球盘口。
- 新增 `server/match_analysis/`：统一获取并标准化历史交锋、积分榜和双方近期
  战绩，默认缓存 6 小时；来源失败时最多返回 7 天内旧缓存并标记 `stale`。
  8 项 Python 单元测试通过。
- 新增 Flutter `MatchAnalysisData` 模型和
  `GET /v1/matches/{id}/analysis` 客户端接入。详情页改为“分析/赔率”两栏；
  完场不再默认跳独立赛果页，只在顶部增加全场、半场结果行。分析有真实数据时
  展示积分排名、历史交锋、主队近期和客队近期；没有数据时保留原有赔率概览，
  不展示空白资料卡。
- `flutter analyze` 已通过。当前受 Codex 沙箱禁止绑定本地测试端口影响，
  `flutter test` 无法启动测试进程，报错为本机 `127.0.0.1:0` socket 权限，
  不是测试断言失败。
- 已通过阿里云轻量服务器命令助手部署到 `/opt/caimaster-api/match_analysis/`，
  并将 `install_match_analysis_routes(app)` 接入线上 `app.py`。部署前备份：
  `/opt/caimaster-api/app.py.bak-pre-match-analysis-20260726-155827`；
  补丁工具另存
  `/opt/caimaster-api/app.py.bak-match-analysis-20260726-155827`。
- `caimaster-api.service` 于 2026-07-26 15:58:27 重启并保持 active。
  本机接口和 `https://api.cclloo.com/v1/matches/2040634/analysis` 均返回 200。
- 已抽查比赛 `2040623` 至 `2040632` 共 10 场：每场的历史交锋、积分榜、
  主队近期和客队近期四个区块均可用，全部 `stale=false`。首次部署任务显示
  失败仅因重启后 35 毫秒立即 smoke 导致短暂连接拒绝；带启动等待的复验成功，
  代码、测试、备份和服务启动本身均已完成。
- 本轮没有安装 App。下一步先在真机详情页检查布局、长队名和空数据降级，再决定
  是否安装；欧赔、亚赔和大小球仍需接入 Titan 或其他有授权的数据源。

## 2026-07-26 计划活跃筛选与首批真实合并

- 公开计划接口新增 `activity=recent|history|all`。`recent` 为近 7 个中国自然日，
  `history` 为更早内容，默认 `all` 保持旧版兼容；使用 `ids` 查询收藏时忽略
  活跃范围，旧收藏不会被时间筛选隐藏。
- App“全部计划”新增“近期活跃 / 历史计划”切换；收藏页保持完整。历史列表使用
  中性的“历史计划”标签，不再把正常历史内容显示成“今日暂未更新”。
- 线上数据核验：部署时活跃计划最后更新分布为 7 月 26 日 135 个、7 月 25 日
  169 个、7 月 24 日 13 个；167 个计划只有一次更新。同步没有整体中断，大量
  未更新主要来自历史上把早晚标题拆成独立计划。
- 按用户确认“带早/晚的一般属于同一方案”，只合并了主体名称明确存在的 9 个：
  `解语花早/晚 -> 解语花`、`豹子豹早/晚 -> 豹子豹`、`狼图腾晚 -> 狼图腾`、
  `云飞飞晚 -> 云飞飞`、`精准晚 -> 精准`、`神灯晚场 -> 神灯`、
  `7.25鸿哥晚场 -> 7.25鸿哥`。共迁移 9 批更新、19 张图片。
- 生产 `plan_aliases` 已有 9 行。旧 ID 查询验证为标准计划，例如 ID `198`
  返回 ID `26` 的“解语花”，并保留别名 `198/319`；同步旧名称“解语花早”
  解析到标准计划 ID `26`。
- 部署前备份：
  `/opt/caimaster-api/plans.db.bak-20260726-205338-activity`、
  `/opt/caimaster-api/plans.py.bak-20260726-205338-activity`、
  `/opt/caimaster-api/app.py.bak-20260726-205338-activity`。执行真实合并前另有
  一份 `plans.db.bak-*-before-explicit-alias-merge`。
- 服务器 5 项活跃筛选和别名合并测试通过；`caimaster-api.service` 与
  `caimaster-plan-source-sync.timer` 均为 active。正式 HTTPS 域名已验证
  `activity=recent` 和旧 ID 重定向。
- 当前计划数据仅覆盖最近 3 天，因此 `history` 暂时为空属于正常现象；数据满
  7 天后会自然出现历史内容。不要为了填充历史页制造演示数据。

## 2026-07-27 计划更新漏单修复

- 用户反馈收藏“平哥”来源已经更新但 App 仍显示未更新。确认来源文章
  `395587 / 20260726平哥早` 被记录为 `pending_name`，因为标准允许名称是
  “平哥”，旧解析器不认识“平哥早”，并非同步服务停机或 App 收藏缓存错误。
- `plan_source_common.py` 新增安全的时段后缀归并：仅当去掉“早、晚、早场、
  晚场、早单、晚单、上午场、下午场、夜场”后的主体名称明确存在于
  `allowedNames` 时才归并。显式别名和完整允许名称优先，陌生主体仍待治理。
- 上线前只读预览确认 7 条可安全处理；部署后精确重试并全部导入：
  平哥早、云飞飞早、启航早/晚、连润早场、苝斗晚、红前串晚。
- 平哥计划 ID `206` 已新增更新 ID `528`，标题 `20260726平哥早`，发布时间
  `2026-07-26 14:46 +08:00`，图片 1 张。正式 HTTPS 与本机 API 均验证成功。
- App 收藏状态文案已改为“今日已更新 / 昨日已更新 / MM-DD 已更新”，不再把
  所有非当天内容笼统显示成“今日暂未更新”。修复版已安装至 `iPhone15`。
- 备份时间戳 `20260727-124050`：`plans.db`、`plan_source_common.py` 和
  `plan_source_sync.py` 均有 `session-suffix` 后缀备份。
- 本地计划服务测试 35 项通过，服务器解析测试 12 项通过，Flutter analyze
  无问题，Web release 构建通过。同步 timer 保持 active。
- 修复后仍有 102 条旧 `pending_name`：101 条来自 7 月 26 日，1 条来自
  7 月 27 日的全新名称“足球大数据”。不要把逗号分隔的多作者标题或未知系列
  自动猜成已有计划；下一步需要设计“作者主体 + 系列名”的可审计解析规则。

## 2026-07-27 用户计划名称分类落地

- 用户在 `计划名称分类标注表_2026-07-27.xlsx` 中完成名称标注，并确认所有
  误填的 `25` 均按空白忽略。
- 按标注安全写入线上名称治理规则：标准允许名称由 318 增至 370，显式别名由
  3 增至 23，忽略名称由 4 增至 5。写入前已备份 `plan_source_map.json`
  与 `plans.db`，备份文件带 `user-classification` 时间戳后缀。
- 重新排队 65 条可确定文章，同步结果为：导入 62、重复 3、失败 0。代表性
  结果：`平局晚 -> 平哥`、`竞胜进球 -> 竞胜`、`毒液以此为准 -> 毒液`、
  `水花有早场 -> 水花儿`、`足球大数据` 作为独立计划导入。
- 同步后 `plan_sync_articles` 剩余 38 条 `pending_name`：36 条为用户确认的
  多作者文章，另外两条为标注表生成后新增的 `智赢，辉煌` 和 `猴子`。
- 多作者文章不能直接写普通别名：同一文章可能包含 2 至 8 张图片，且图片数量
  经常与作者数量不同。当前同步器仍是一篇文章归一个计划，强行映射会把不同作者
  图片混入同一计划。下一步应增加可审计的图片级拆分规则，再处理这 36 条。
- 线上 `caimaster-api.service` 和 `caimaster-plan-source-sync.timer` 均为
  active；正式 HTTPS 的计划 recent/list/updates 冒烟检查全部通过。

## 2026-07-27 多作者图片级拆分底层能力

- 名称规则新增 `imageAssignments`：按来源文章原始名称配置每一张图片对应的
  标准计划名称，例如 `["辉哥", "扫地僧"]`。
- 同步器现在可以在一个事务中为同一篇文章创建多个计划更新，并把图片分别写入
  对应作者；新增 `plan_sync_article_updates` 表保存文章与全部更新的关联。
- 图片数量必须与配置列表完全一致。不一致时返回
  `image assignment awaits review`，文章继续处于 `pending_name`，不会部分导入、
  猜测作者或污染已有计划。
- 单作者同步路径保持原行为。服务器隔离测试 18 项通过，线上真实来源只读预演：
  295 篇候选、246 篇有效、失败 0；未配置拆分规则时仍保留 38 条待治理。
- 上线前备份了 `plan_source_common.py`、`plan_source_sync.py`、`plans.py` 和
  `plans.db`，备份文件带 `image-assignments` 时间戳后缀。
- 线上底层能力已部署，但尚未给任何真实多作者名称写入 `imageAssignments`；
  因此不会自动处理那 36 条多作者历史文章。下一步需根据图片文字识别结果，
  逐条生成并复核图片顺序配置，再分批启用。

## 2026-07-27 多作者图片级拆分生产落地

- 已分两批给 26 个原始多作者名称配置 `imageAssignments`。首批含
  `扫地僧，辉哥中赔`、`木鱼，追梦`、`章鱼哥，左岸` 等 15 条；第二批含
  `人工智能，皇冠`、`好运来，旺旺`、`老彩民，飞跃，牛头，飞虎队`、
  `南枝，海尔，情人`、`智赢，辉煌` 等 11 条。
- 每批操作前均备份 `plan_source_map.json` 与 `plans.db`，备份文件分别带
  `split-pilot`、`split-batch1`、`split-batch2` 时间戳后缀。没有删除或覆盖
  旧计划，也没有对无法看清作者归属的图片做自动猜测。
- 第二批 11 篇全部导入成功、重复 0、失败 0；共生成 25 个作者更新并保留
  47 张原图。逐篇反查的更新数、图片数和配置完全一致。正式 HTTPS 冒烟通过，
  `caimaster-api.service` 与 `caimaster-plan-source-sync.timer` 均为 active。
- 当前仅剩 12 条 `pending_name`：
  `五月花相遇，稳中中`、`企鹅满地红`、`幻影新起点`、`胜利百灵鸟`、
  `小米柒月`、`大白鲨蓝天系列`、`胖哥禧悦`、`行者当家`、
  `平头江山如画`、`解语花红豆`、`小爱同学神之一手晚`、`猴子`。
  前 11 条多为没有分隔符的多作者拼接名，需要重新下载图片并按图片文字确认；
  `猴子` 尚无用户分类结论，继续保留待审核。

## 2026-07-27 剩余逐图分类与历史错图清理

- 用户完成 `待确认计划逐图分类表_2026-07-27.xlsx`。剩余 12 篇已按每张图片
  的确认名称配置并导入，共写入 51 张有效图片；`小米柒月`第 2 张明确标记为
  忽略，没有下载或写入任何计划。
- 同步规则新增内部图片标记 `__IGNORE__`。图片数量仍必须与规则严格一致，
  忽略项不进入允许名称、不参与去重、不创建计划/更新/媒体记录。服务器解析及
  事务测试共 19 项通过。
- 导入时发现 `395594 幻影新起点` 的 4 张图片此前被来源重复内容误写进
  `395593 鸿运当头` 的更新 ID `395`。安全检查确认该更新共有 19 张图，只
  精确删除错误图片 ID `1092-1095` 及其媒体文件，保留其余 15 张正确图片；
  随后 4 张图片已重新导入“幻影”。
- 最终 12 篇状态全部为 `imported`，正式 HTTPS 冒烟通过，
  `caimaster-api.service` 与 `caimaster-plan-source-sync.timer` 均为 active。
  按用户要求，本轮调试数据修正未额外创建备份。
- 同步后又出现 12 条新 `pending_name`，属于后续新文章，不是本轮导入失败：
  `胖哥，禧悦`、`耕耘足球`、`老彩民，飞跃`、`惊鸿`、`企鹅`、
  `大白鲨蓝天`、`阿里山，豫西北，伟哥`、`彤宝系列`、`福地系列`、
  `百灵鸟`、`星光半场，红冠`、`红中吉利`。

## 2026-07-27 全库一名一计划治理

- 用户完成 `计划全库混合与重复分类表_2026-07-27.xlsx`。主表 187 张图片中
  186 张迁移到确认作者，1 张无法辨认的“大吉大利”图片设为 inactive，
  保留原记录但不再公开展示。
- 图片迁移前只读预演严格核对 187 个图片 ID、53 个更新和 42 个旧计划，
  覆盖率完全一致后才执行。共创建 114 个按作者拆分的更新，没有复制媒体文件，
  直接迁移原图片记录；事务完成后逐图反查错误数为 0、孤立活跃图片为 0。
- 10 组同作者重复计划已归并到标准名称：今日抄底、星光、桂花、牛总、
  狼图腾、米兰、老宋、虎啸、追梦、辉哥。共停用并建立别名重定向 14 个旧
  计划 ID，迁移 19 个更新，旧收藏仍可通过 alias 解析到标准计划。
- 复核发现初始审核清单漏了 3 个旧混合计划，已按原图补拆：
  `特殊指数，日期更正 -> 特殊指数`；
  `桂花早，飞龙在天 -> 桂花 / 飞龙在天`；
  `幻影，新起点 -> 新起点 / 幻影`。共迁移 8 张图片、创建 6 个更新。
- 线上名称规则已收紧：41 个旧混合名称从 `allowedNames` 移除；重复系列名
  增加到标准作者的显式 alias；最后 3 个来源名称增加严格图片顺序规则。
  未知或图片数量变化会重新进入待审核，不会自动生成混合计划。
- 最终检查：活跃混合名称数量 0、活跃同名重复数量 0、孤立活跃图片数量 0；
  active 计划 384 个，`plan_aliases` 23 行。正式 HTTPS 冒烟通过，
  `caimaster-api.service` 与 `caimaster-plan-source-sync.timer` 均 active。
  按用户要求，本轮调试数据治理没有额外创建数据库备份。

## 2026-07-27 停用多作者顺序分类

- 用户指出每天可能缺少早场或晚场，图片数量和顺序不固定，因此按位置套用
  `imageAssignments` 会把作者分错。该判断正确，顺序规则不再用于日常同步。
- 生产 `plan_source_map.json` 中 41 条活动 `imageAssignments` 已全部移入
  `archivedOrderedImageAssignments`，活动规则数量为 0；对应组合名称同时从
  `allowedNames` 和 aliases 键中移除。归档仅供审计，不参与解析。
- 停用后同步验证：新文章 `桂花，情人`、`飞龙在天，喜盈盈`、
  `任姓小姐姐` 均进入 `pending_name`，没有自动创建或写入混合计划。
- 审计到停用前最近触发顺序规则的文章只有 `395859 灵蛇，西瓜` 和
  `395932 幻影，新起点`。后者已在全库治理中按图重新拆分；前者逐图复核确认
  4 张均为西瓜足球、1 张为灵蛇，现有归属正确，无需回滚。
- 下一阶段自动化必须改为逐图内容识别：文章标题只提供候选作者，每张图片独立
  OCR/模板识别，高置信度才写入，低置信度继续保持 `pending_name`；不得再以
  图片位置或数量作为作者归属依据。

## 2026-07-28 比赛数据自动巡检

- 新增 `server/match_data_audit.py`，交叉检查今日、首页和完场接口，包括重复
  ID、状态冲突、完场缺少赛果、半场比分超过全场比分、官方赛果不一致、
  进行中超过四小时，以及球探实时更新时间超过五分钟。
- 服务器已安装并启用 `caimaster-match-data-audit.timer`，每五分钟运行一次
  `caimaster-match-data-audit.service`。脚本路径为
  `/opt/caimaster-api/match_data_audit.py`。
- 首次线上审计：今日 2 场、首页 14 场、完场 149 场，错误 0、警告 0。
- 检查定时器：
  `systemctl list-timers caimaster-match-data-audit.timer --no-pager`
- 查看最近结果：
  `journalctl -u caimaster-match-data-audit.service -n 80 --no-pager`
- 手动复核：
  `/opt/caimaster-api/.venv/bin/python3.11 /opt/caimaster-api/match_data_audit.py --base-url https://api.cclloo.com`
