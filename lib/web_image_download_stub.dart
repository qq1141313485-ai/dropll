import 'dart:typed_data';

bool downloadImageUrl(String url, {required String fileName}) => false;

bool downloadImageBytes(
  Uint8List bytes, {
  required String mimeType,
  required String fileName,
}) =>
    false;
